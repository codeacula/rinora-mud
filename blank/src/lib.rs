use shared::prelude::*;

mod commands;
mod output;
mod systems;

pub struct NewPlugin;

impl Plugin for NewPlugin {
    fn build(&self, app: &mut App) {
        todo!("Add your systems and stuff here.")
    }
}
