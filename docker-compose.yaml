version: '3'
services:
  db:
    image: mongo
    restart: always
    environment:
        MONGO_INITDB_ROOT_USERNAME: dev
        MONGO_INITDB_ROOT_PASSWORD: dev
    volumes:
      - type: bind
        source: ./.private/mongodb
        target: /data/db
    ports:
      - 27017:27017c